{
  "name": "@auraui/router-playground",
  "private": true,
  "version": "0.2.0",
  "type": "module",
  "description": "Minimal MPA playground for @auraui/router — open in StackBlitz or run locally.",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview"
  },
  "dependencies": {
    "@auraui/router": "0.2.0"
  },
  "devDependencies": {
    "vite": "^6.3.5"
  }
}
